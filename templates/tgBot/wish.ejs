<%- include('./blocks/firstName', { $t, userData }); %>

<% if (canBuy) { %>
    , <%- $t('wish.dropped'); %>:<br>

    <% if (Number(newItemData.rarity) === 5) { %>
        <b>
            <i>
                <%- include('./blocks/newItemRow', { newItemData }); %>
            </i>
        </b>
    <% } else if (Number(newItemData.rarity) === 4) { %>
        <b>
            <%- include('./blocks/newItemRow', { newItemData }); %>
        </b>
    <% } else { %>
        <%- include('./blocks/newItemRow', { newItemData }); %>
    <% } %>

    <% if (cashBackTemplate) { %>
        , +<%= cashBackTemplate; %>
    <% } %>

    <br>
    <br>

    <%= newItemData.description; %>

    <br>
    <br>

    🔸<i><%= bannerName; %></i>

    <br>
    <br>

    <%- include('./blocks/spendCurrency', { $t, templatePrices }); %>
<% } else { %>
    ! <%- $t(`wish.noPrimogems.${currentBannerType}`)%>
<% } %>
